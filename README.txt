AnimeChat AI (WebUI)

1) Install dependencies:
   pip install -r requirements.txt

2) Run the app:
   python webui.py

First run will show a setup screen. Provide the GGUF chat model path and the ONNX voice model path.
All settings are stored in config.json next to webui.py.
